# Auth service

Planned service responsibility: validate Supabase identities, resolve application roles, and propagate authorization context. No runnable Auth service or gateway JWT validation is implemented. The web workspace has Supabase client helpers and session-refresh proxy plumbing only; login, callbacks and tenant roles remain pending.
