# Services

Each service exposes `/health` and `/metrics`, owns only its domain data, and must publish/consume only documented contracts. The folders are intentionally small bootstrap targets, not placeholder claims of a completed distributed system.
