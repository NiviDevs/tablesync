# Contributing

Create focused changes, run the relevant checks, and keep documentation in sync with observable behavior. Do not commit secrets. Changes to events or inter-service APIs must update `docs/EVENTS.md`.
