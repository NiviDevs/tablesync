export default function Ops(){const bars=[32,43,37,54,49,72,64,85,70,94,76,89];return <main className="ops"><div className="portal"><nav className="portal-nav"><div className="brand">table<i>sync</i> / control</div><span className="eyebrow">local environment</span><span className="signal" style={{marginLeft:'auto'}}>● system shell online</span></nav><p className="eyebrow" style={{marginTop:28}}>Operations control plane</p><h1 style={{letterSpacing:'-2px'}}>System overview</h1><section className="stat-grid">{[['—','Request rate'],['—','p95 latency'],['—','Queue depth'],['—','Active sessions']].map(([n,l])=><div className="stat" key={l}><span className="eyebrow">{l}</span><strong>{n}</strong><small className="warning">Awaiting Prometheus connection</small></div>)}</section><section style={{display:'grid',gridTemplateColumns:'1.2fr .8fr',gap:16}}><div className="stat"><span className="eyebrow">Traffic / real metrics required</span><div className="chart">{bars.map((h,i)=><div key={i} className="bar" style={{height:h+'%'}} />)}</div><small className="warning">Preview visual only — not telemetry</small></div><div className="stat"><span className="eyebrow">Service topology</span>{['api-gateway','session','order','payment','restaurant'].map((x,i)=><div className="event" key={x}><span className={i===0?'signal':'warning'}>●</span> {x} <span style={{float:'right'}}>{i===0?'ready':'not connected'}</span></div>)}</div></section><section className="stat" style={{marginTop:16}}><span className="eyebrow">Event stream / contract preview</span>{['participant.joined.v1','cart.item_added.v1','payment.succeeded.v1','order.confirmed.v1'].map(e=><div className="event" key={e}><span className="signal">→</span> {e}<span style={{float:'right',color:'#777'}}>no broker connection</span></div>)}</section></div></main>}
